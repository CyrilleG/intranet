// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package intranet;

import intranet.Group;
import intranet.GroupDataOnDemand;
import intranet.GroupIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect GroupIntegrationTest_Roo_IntegrationTest {
    
    declare @type: GroupIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: GroupIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: GroupIntegrationTest: @Transactional;
    
    @Autowired
    private GroupDataOnDemand GroupIntegrationTest.dod;
    
    @Test
    public void GroupIntegrationTest.testCountGroups() {
        Assert.assertNotNull("Data on demand for 'Group' failed to initialize correctly", dod.getRandomGroup());
        long count = Group.countGroups();
        Assert.assertTrue("Counter for 'Group' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void GroupIntegrationTest.testFindGroup() {
        Group obj = dod.getRandomGroup();
        Assert.assertNotNull("Data on demand for 'Group' failed to initialize correctly", obj);
        Integer id = obj.getIdgroup();
        Assert.assertNotNull("Data on demand for 'Group' failed to provide an identifier", id);
        obj = Group.findGroup(id);
        Assert.assertNotNull("Find method for 'Group' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Group' returned the incorrect identifier", id, obj.getIdgroup());
    }
    
    @Test
    public void GroupIntegrationTest.testFindAllGroups() {
        Assert.assertNotNull("Data on demand for 'Group' failed to initialize correctly", dod.getRandomGroup());
        long count = Group.countGroups();
        Assert.assertTrue("Too expensive to perform a find all test for 'Group', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Group> result = Group.findAllGroups();
        Assert.assertNotNull("Find all method for 'Group' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Group' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void GroupIntegrationTest.testFindGroupEntries() {
        Assert.assertNotNull("Data on demand for 'Group' failed to initialize correctly", dod.getRandomGroup());
        long count = Group.countGroups();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Group> result = Group.findGroupEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Group' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Group' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void GroupIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'Group' failed to initialize correctly", dod.getRandomGroup());
        Group obj = dod.getNewTransientGroup(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Group' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Group' identifier to be null", obj.getIdgroup());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'Group' identifier to no longer be null", obj.getIdgroup());
    }
    
    @Test
    public void GroupIntegrationTest.testRemove() {
        Group obj = dod.getRandomGroup();
        Assert.assertNotNull("Data on demand for 'Group' failed to initialize correctly", obj);
        Integer id = obj.getIdgroup();
        Assert.assertNotNull("Data on demand for 'Group' failed to provide an identifier", id);
        obj = Group.findGroup(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'Group' with identifier '" + id + "'", Group.findGroup(id));
    }
    
}
