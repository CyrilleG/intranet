// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package intranet;

import intranet.UserRights;
import intranet.UserRightsDataOnDemand;
import intranet.UserRightsIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect UserRightsIntegrationTest_Roo_IntegrationTest {
    
    declare @type: UserRightsIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: UserRightsIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: UserRightsIntegrationTest: @Transactional;
    
    @Autowired
    private UserRightsDataOnDemand UserRightsIntegrationTest.dod;
    
    @Test
    public void UserRightsIntegrationTest.testCountUserRightses() {
        Assert.assertNotNull("Data on demand for 'UserRights' failed to initialize correctly", dod.getRandomUserRights());
        long count = UserRights.countUserRightses();
        Assert.assertTrue("Counter for 'UserRights' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void UserRightsIntegrationTest.testFindUserRights() {
        UserRights obj = dod.getRandomUserRights();
        Assert.assertNotNull("Data on demand for 'UserRights' failed to initialize correctly", obj);
        Integer id = obj.getIduserRights();
        Assert.assertNotNull("Data on demand for 'UserRights' failed to provide an identifier", id);
        obj = UserRights.findUserRights(id);
        Assert.assertNotNull("Find method for 'UserRights' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'UserRights' returned the incorrect identifier", id, obj.getIduserRights());
    }
    
    @Test
    public void UserRightsIntegrationTest.testFindAllUserRightses() {
        Assert.assertNotNull("Data on demand for 'UserRights' failed to initialize correctly", dod.getRandomUserRights());
        long count = UserRights.countUserRightses();
        Assert.assertTrue("Too expensive to perform a find all test for 'UserRights', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<UserRights> result = UserRights.findAllUserRightses();
        Assert.assertNotNull("Find all method for 'UserRights' illegally returned null", result);
        Assert.assertTrue("Find all method for 'UserRights' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void UserRightsIntegrationTest.testFindUserRightsEntries() {
        Assert.assertNotNull("Data on demand for 'UserRights' failed to initialize correctly", dod.getRandomUserRights());
        long count = UserRights.countUserRightses();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<UserRights> result = UserRights.findUserRightsEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'UserRights' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'UserRights' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void UserRightsIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'UserRights' failed to initialize correctly", dod.getRandomUserRights());
        UserRights obj = dod.getNewTransientUserRights(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'UserRights' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'UserRights' identifier to be null", obj.getIduserRights());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'UserRights' identifier to no longer be null", obj.getIduserRights());
    }
    
    @Test
    public void UserRightsIntegrationTest.testRemove() {
        UserRights obj = dod.getRandomUserRights();
        Assert.assertNotNull("Data on demand for 'UserRights' failed to initialize correctly", obj);
        Integer id = obj.getIduserRights();
        Assert.assertNotNull("Data on demand for 'UserRights' failed to provide an identifier", id);
        obj = UserRights.findUserRights(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'UserRights' with identifier '" + id + "'", UserRights.findUserRights(id));
    }
    
}
