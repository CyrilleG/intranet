// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package intranet;

import intranet.GroupRights;
import intranet.GroupRightsDataOnDemand;
import intranet.GroupRightsIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect GroupRightsIntegrationTest_Roo_IntegrationTest {
    
    declare @type: GroupRightsIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: GroupRightsIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: GroupRightsIntegrationTest: @Transactional;
    
    @Autowired
    private GroupRightsDataOnDemand GroupRightsIntegrationTest.dod;
    
    @Test
    public void GroupRightsIntegrationTest.testCountGroupRightses() {
        Assert.assertNotNull("Data on demand for 'GroupRights' failed to initialize correctly", dod.getRandomGroupRights());
        long count = GroupRights.countGroupRightses();
        Assert.assertTrue("Counter for 'GroupRights' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void GroupRightsIntegrationTest.testFindGroupRights() {
        GroupRights obj = dod.getRandomGroupRights();
        Assert.assertNotNull("Data on demand for 'GroupRights' failed to initialize correctly", obj);
        Integer id = obj.getIdgroupRights();
        Assert.assertNotNull("Data on demand for 'GroupRights' failed to provide an identifier", id);
        obj = GroupRights.findGroupRights(id);
        Assert.assertNotNull("Find method for 'GroupRights' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'GroupRights' returned the incorrect identifier", id, obj.getIdgroupRights());
    }
    
    @Test
    public void GroupRightsIntegrationTest.testFindAllGroupRightses() {
        Assert.assertNotNull("Data on demand for 'GroupRights' failed to initialize correctly", dod.getRandomGroupRights());
        long count = GroupRights.countGroupRightses();
        Assert.assertTrue("Too expensive to perform a find all test for 'GroupRights', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<GroupRights> result = GroupRights.findAllGroupRightses();
        Assert.assertNotNull("Find all method for 'GroupRights' illegally returned null", result);
        Assert.assertTrue("Find all method for 'GroupRights' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void GroupRightsIntegrationTest.testFindGroupRightsEntries() {
        Assert.assertNotNull("Data on demand for 'GroupRights' failed to initialize correctly", dod.getRandomGroupRights());
        long count = GroupRights.countGroupRightses();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<GroupRights> result = GroupRights.findGroupRightsEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'GroupRights' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'GroupRights' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void GroupRightsIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'GroupRights' failed to initialize correctly", dod.getRandomGroupRights());
        GroupRights obj = dod.getNewTransientGroupRights(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'GroupRights' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'GroupRights' identifier to be null", obj.getIdgroupRights());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'GroupRights' identifier to no longer be null", obj.getIdgroupRights());
    }
    
    @Test
    public void GroupRightsIntegrationTest.testRemove() {
        GroupRights obj = dod.getRandomGroupRights();
        Assert.assertNotNull("Data on demand for 'GroupRights' failed to initialize correctly", obj);
        Integer id = obj.getIdgroupRights();
        Assert.assertNotNull("Data on demand for 'GroupRights' failed to provide an identifier", id);
        obj = GroupRights.findGroupRights(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'GroupRights' with identifier '" + id + "'", GroupRights.findGroupRights(id));
    }
    
}
