// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package intranet;

import intranet.Group;
import intranet.GroupDataOnDemand;
import intranet.User;
import intranet.UserDataOnDemand;
import intranet.UserGroups;
import intranet.UserGroupsDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

privileged aspect UserGroupsDataOnDemand_Roo_DataOnDemand {
    
    declare @type: UserGroupsDataOnDemand: @Component;
    
    private Random UserGroupsDataOnDemand.rnd = new SecureRandom();
    
    private List<UserGroups> UserGroupsDataOnDemand.data;
    
    @Autowired
    private GroupDataOnDemand UserGroupsDataOnDemand.groupDataOnDemand;
    
    @Autowired
    private UserDataOnDemand UserGroupsDataOnDemand.userDataOnDemand;
    
    public UserGroups UserGroupsDataOnDemand.getNewTransientUserGroups(int index) {
        UserGroups obj = new UserGroups();
        setIdgroup(obj, index);
        setIduser(obj, index);
        return obj;
    }
    
    public void UserGroupsDataOnDemand.setIdgroup(UserGroups obj, int index) {
        Group idgroup = groupDataOnDemand.getRandomGroup();
        obj.setIdgroup(idgroup);
    }
    
    public void UserGroupsDataOnDemand.setIduser(UserGroups obj, int index) {
        User iduser = userDataOnDemand.getRandomUser();
        obj.setIduser(iduser);
    }
    
    public UserGroups UserGroupsDataOnDemand.getSpecificUserGroups(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        UserGroups obj = data.get(index);
        Integer id = obj.getIduserGroups();
        return UserGroups.findUserGroups(id);
    }
    
    public UserGroups UserGroupsDataOnDemand.getRandomUserGroups() {
        init();
        UserGroups obj = data.get(rnd.nextInt(data.size()));
        Integer id = obj.getIduserGroups();
        return UserGroups.findUserGroups(id);
    }
    
    public boolean UserGroupsDataOnDemand.modifyUserGroups(UserGroups obj) {
        return false;
    }
    
    public void UserGroupsDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = UserGroups.findUserGroupsEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'UserGroups' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<UserGroups>();
        for (int i = 0; i < 10; i++) {
            UserGroups obj = getNewTransientUserGroups(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
