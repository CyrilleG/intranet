// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package intranet;

import intranet.AppGroup;
import intranet.AppGroupDataOnDemand;
import intranet.DataGroup;
import intranet.DataGroupDataOnDemand;
import intranet.ModuleData;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

privileged aspect DataGroupDataOnDemand_Roo_DataOnDemand {
    
    declare @type: DataGroupDataOnDemand: @Component;
    
    private Random DataGroupDataOnDemand.rnd = new SecureRandom();
    
    private List<DataGroup> DataGroupDataOnDemand.data;
    
    @Autowired
    private AppGroupDataOnDemand DataGroupDataOnDemand.appGroupDataOnDemand;
    
    public DataGroup DataGroupDataOnDemand.getNewTransientDataGroup(int index) {
        DataGroup obj = new DataGroup();
        setData(obj, index);
        setGroup(obj, index);
        return obj;
    }
    
    public void DataGroupDataOnDemand.setData(DataGroup obj, int index) {
        ModuleData data = null;
        obj.setData(data);
    }
    
    public void DataGroupDataOnDemand.setGroup(DataGroup obj, int index) {
        AppGroup group = appGroupDataOnDemand.getRandomAppGroup();
        obj.setGroup(group);
    }
    
    public DataGroup DataGroupDataOnDemand.getSpecificDataGroup(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        DataGroup obj = data.get(index);
        Integer id = obj.getDataGroup();
        return DataGroup.findDataGroup(id);
    }
    
    public DataGroup DataGroupDataOnDemand.getRandomDataGroup() {
        init();
        DataGroup obj = data.get(rnd.nextInt(data.size()));
        Integer id = obj.getDataGroup();
        return DataGroup.findDataGroup(id);
    }
    
    public boolean DataGroupDataOnDemand.modifyDataGroup(DataGroup obj) {
        return false;
    }
    
    public void DataGroupDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = DataGroup.findDataGroupEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'DataGroup' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<DataGroup>();
        for (int i = 0; i < 10; i++) {
            DataGroup obj = getNewTransientDataGroup(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
