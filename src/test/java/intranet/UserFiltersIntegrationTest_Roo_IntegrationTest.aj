// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package intranet;

import intranet.UserFilters;
import intranet.UserFiltersDataOnDemand;
import intranet.UserFiltersIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect UserFiltersIntegrationTest_Roo_IntegrationTest {
    
    declare @type: UserFiltersIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: UserFiltersIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: UserFiltersIntegrationTest: @Transactional;
    
    @Autowired
    private UserFiltersDataOnDemand UserFiltersIntegrationTest.dod;
    
    @Test
    public void UserFiltersIntegrationTest.testCountUserFilterses() {
        Assert.assertNotNull("Data on demand for 'UserFilters' failed to initialize correctly", dod.getRandomUserFilters());
        long count = UserFilters.countUserFilterses();
        Assert.assertTrue("Counter for 'UserFilters' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void UserFiltersIntegrationTest.testFindUserFilters() {
        UserFilters obj = dod.getRandomUserFilters();
        Assert.assertNotNull("Data on demand for 'UserFilters' failed to initialize correctly", obj);
        Integer id = obj.getIduserFilters();
        Assert.assertNotNull("Data on demand for 'UserFilters' failed to provide an identifier", id);
        obj = UserFilters.findUserFilters(id);
        Assert.assertNotNull("Find method for 'UserFilters' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'UserFilters' returned the incorrect identifier", id, obj.getIduserFilters());
    }
    
    @Test
    public void UserFiltersIntegrationTest.testFindAllUserFilterses() {
        Assert.assertNotNull("Data on demand for 'UserFilters' failed to initialize correctly", dod.getRandomUserFilters());
        long count = UserFilters.countUserFilterses();
        Assert.assertTrue("Too expensive to perform a find all test for 'UserFilters', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<UserFilters> result = UserFilters.findAllUserFilterses();
        Assert.assertNotNull("Find all method for 'UserFilters' illegally returned null", result);
        Assert.assertTrue("Find all method for 'UserFilters' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void UserFiltersIntegrationTest.testFindUserFiltersEntries() {
        Assert.assertNotNull("Data on demand for 'UserFilters' failed to initialize correctly", dod.getRandomUserFilters());
        long count = UserFilters.countUserFilterses();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<UserFilters> result = UserFilters.findUserFiltersEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'UserFilters' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'UserFilters' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void UserFiltersIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'UserFilters' failed to initialize correctly", dod.getRandomUserFilters());
        UserFilters obj = dod.getNewTransientUserFilters(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'UserFilters' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'UserFilters' identifier to be null", obj.getIduserFilters());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'UserFilters' identifier to no longer be null", obj.getIduserFilters());
    }
    
    @Test
    public void UserFiltersIntegrationTest.testRemove() {
        UserFilters obj = dod.getRandomUserFilters();
        Assert.assertNotNull("Data on demand for 'UserFilters' failed to initialize correctly", obj);
        Integer id = obj.getIduserFilters();
        Assert.assertNotNull("Data on demand for 'UserFilters' failed to provide an identifier", id);
        obj = UserFilters.findUserFilters(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'UserFilters' with identifier '" + id + "'", UserFilters.findUserFilters(id));
    }
    
}
