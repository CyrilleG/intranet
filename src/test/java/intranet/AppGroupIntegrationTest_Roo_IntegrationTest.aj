// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package intranet;

import intranet.AppGroup;
import intranet.AppGroupDataOnDemand;
import intranet.AppGroupIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect AppGroupIntegrationTest_Roo_IntegrationTest {
    
    declare @type: AppGroupIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: AppGroupIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: AppGroupIntegrationTest: @Transactional;
    
    @Autowired
    private AppGroupDataOnDemand AppGroupIntegrationTest.dod;
    
    @Test
    public void AppGroupIntegrationTest.testCountAppGroups() {
        Assert.assertNotNull("Data on demand for 'AppGroup' failed to initialize correctly", dod.getRandomAppGroup());
        long count = AppGroup.countAppGroups();
        Assert.assertTrue("Counter for 'AppGroup' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void AppGroupIntegrationTest.testFindAppGroup() {
        AppGroup obj = dod.getRandomAppGroup();
        Assert.assertNotNull("Data on demand for 'AppGroup' failed to initialize correctly", obj);
        Integer id = obj.getGroup();
        Assert.assertNotNull("Data on demand for 'AppGroup' failed to provide an identifier", id);
        obj = AppGroup.findAppGroup(id);
        Assert.assertNotNull("Find method for 'AppGroup' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'AppGroup' returned the incorrect identifier", id, obj.getGroup());
    }
    
    @Test
    public void AppGroupIntegrationTest.testFindAllAppGroups() {
        Assert.assertNotNull("Data on demand for 'AppGroup' failed to initialize correctly", dod.getRandomAppGroup());
        long count = AppGroup.countAppGroups();
        Assert.assertTrue("Too expensive to perform a find all test for 'AppGroup', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<AppGroup> result = AppGroup.findAllAppGroups();
        Assert.assertNotNull("Find all method for 'AppGroup' illegally returned null", result);
        Assert.assertTrue("Find all method for 'AppGroup' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void AppGroupIntegrationTest.testFindAppGroupEntries() {
        Assert.assertNotNull("Data on demand for 'AppGroup' failed to initialize correctly", dod.getRandomAppGroup());
        long count = AppGroup.countAppGroups();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<AppGroup> result = AppGroup.findAppGroupEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'AppGroup' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'AppGroup' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void AppGroupIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'AppGroup' failed to initialize correctly", dod.getRandomAppGroup());
        AppGroup obj = dod.getNewTransientAppGroup(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'AppGroup' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'AppGroup' identifier to be null", obj.getGroup());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'AppGroup' identifier to no longer be null", obj.getGroup());
    }
    
    @Test
    public void AppGroupIntegrationTest.testRemove() {
        AppGroup obj = dod.getRandomAppGroup();
        Assert.assertNotNull("Data on demand for 'AppGroup' failed to initialize correctly", obj);
        Integer id = obj.getGroup();
        Assert.assertNotNull("Data on demand for 'AppGroup' failed to provide an identifier", id);
        obj = AppGroup.findAppGroup(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'AppGroup' with identifier '" + id + "'", AppGroup.findAppGroup(id));
    }
    
}
