// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package intranet;

import intranet.Fields;
import intranet.FieldsDataOnDemand;
import intranet.Objects;
import intranet.ObjectsDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

privileged aspect FieldsDataOnDemand_Roo_DataOnDemand {
    
    declare @type: FieldsDataOnDemand: @Component;
    
    private Random FieldsDataOnDemand.rnd = new SecureRandom();
    
    private List<Fields> FieldsDataOnDemand.data;
    
    @Autowired
    private ObjectsDataOnDemand FieldsDataOnDemand.objectsDataOnDemand;
    
    public Fields FieldsDataOnDemand.getNewTransientFields(int index) {
        Fields obj = new Fields();
        setIdobject(obj, index);
        setName(obj, index);
        setValue(obj, index);
        return obj;
    }
    
    public void FieldsDataOnDemand.setIdobject(Fields obj, int index) {
        Objects idobject = objectsDataOnDemand.getRandomObjects();
        obj.setIdobject(idobject);
    }
    
    public void FieldsDataOnDemand.setName(Fields obj, int index) {
        String name = "name_" + index;
        if (name.length() > 100) {
            name = name.substring(0, 100);
        }
        obj.setName(name);
    }
    
    public void FieldsDataOnDemand.setValue(Fields obj, int index) {
        String value = "value_" + index;
        if (value.length() > 255) {
            value = value.substring(0, 255);
        }
        obj.setValue(value);
    }
    
    public Fields FieldsDataOnDemand.getSpecificFields(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        Fields obj = data.get(index);
        Integer id = obj.getIdfields();
        return Fields.findFields(id);
    }
    
    public Fields FieldsDataOnDemand.getRandomFields() {
        init();
        Fields obj = data.get(rnd.nextInt(data.size()));
        Integer id = obj.getIdfields();
        return Fields.findFields(id);
    }
    
    public boolean FieldsDataOnDemand.modifyFields(Fields obj) {
        return false;
    }
    
    public void FieldsDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = Fields.findFieldsEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'Fields' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<Fields>();
        for (int i = 0; i < 10; i++) {
            Fields obj = getNewTransientFields(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
