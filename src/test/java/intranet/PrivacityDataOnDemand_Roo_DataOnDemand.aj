// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package intranet;

import intranet.Group;
import intranet.GroupDataOnDemand;
import intranet.Privacity;
import intranet.PrivacityDataOnDemand;
import intranet.User;
import intranet.UserDataOnDemand;
import intranet.UserInfo;
import intranet.UserInfoDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

privileged aspect PrivacityDataOnDemand_Roo_DataOnDemand {
    
    declare @type: PrivacityDataOnDemand: @Component;
    
    private Random PrivacityDataOnDemand.rnd = new SecureRandom();
    
    private List<Privacity> PrivacityDataOnDemand.data;
    
    @Autowired
    private GroupDataOnDemand PrivacityDataOnDemand.groupDataOnDemand;
    
    @Autowired
    private UserInfoDataOnDemand PrivacityDataOnDemand.userInfoDataOnDemand;
    
    @Autowired
    private UserDataOnDemand PrivacityDataOnDemand.userDataOnDemand;
    
    public Privacity PrivacityDataOnDemand.getNewTransientPrivacity(int index) {
        Privacity obj = new Privacity();
        setIdgroup(obj, index);
        setIdinfo(obj, index);
        setIduser(obj, index);
        return obj;
    }
    
    public void PrivacityDataOnDemand.setIdgroup(Privacity obj, int index) {
        Group idgroup = groupDataOnDemand.getRandomGroup();
        obj.setIdgroup(idgroup);
    }
    
    public void PrivacityDataOnDemand.setIdinfo(Privacity obj, int index) {
        UserInfo idinfo = userInfoDataOnDemand.getRandomUserInfo();
        obj.setIdinfo(idinfo);
    }
    
    public void PrivacityDataOnDemand.setIduser(Privacity obj, int index) {
        User iduser = userDataOnDemand.getRandomUser();
        obj.setIduser(iduser);
    }
    
    public Privacity PrivacityDataOnDemand.getSpecificPrivacity(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        Privacity obj = data.get(index);
        Integer id = obj.getIdprivacity();
        return Privacity.findPrivacity(id);
    }
    
    public Privacity PrivacityDataOnDemand.getRandomPrivacity() {
        init();
        Privacity obj = data.get(rnd.nextInt(data.size()));
        Integer id = obj.getIdprivacity();
        return Privacity.findPrivacity(id);
    }
    
    public boolean PrivacityDataOnDemand.modifyPrivacity(Privacity obj) {
        return false;
    }
    
    public void PrivacityDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = Privacity.findPrivacityEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'Privacity' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<Privacity>();
        for (int i = 0; i < 10; i++) {
            Privacity obj = getNewTransientPrivacity(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
