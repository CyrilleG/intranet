// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package intranet;

import intranet.Objects;
import intranet.ObjectsDataOnDemand;
import intranet.User;
import intranet.UserDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

privileged aspect ObjectsDataOnDemand_Roo_DataOnDemand {
    
    declare @type: ObjectsDataOnDemand: @Component;
    
    private Random ObjectsDataOnDemand.rnd = new SecureRandom();
    
    private List<Objects> ObjectsDataOnDemand.data;
    
    @Autowired
    private UserDataOnDemand ObjectsDataOnDemand.userDataOnDemand;
    
    public Objects ObjectsDataOnDemand.getNewTransientObjects(int index) {
        Objects obj = new Objects();
        setIduser(obj, index);
        setName(obj, index);
        return obj;
    }
    
    public void ObjectsDataOnDemand.setIduser(Objects obj, int index) {
        User iduser = userDataOnDemand.getRandomUser();
        obj.setIduser(iduser);
    }
    
    public void ObjectsDataOnDemand.setName(Objects obj, int index) {
        String name = "name_" + index;
        if (name.length() > 100) {
            name = name.substring(0, 100);
        }
        obj.setName(name);
    }
    
    public Objects ObjectsDataOnDemand.getSpecificObjects(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        Objects obj = data.get(index);
        Integer id = obj.getIdobject();
        return Objects.findObjects(id);
    }
    
    public Objects ObjectsDataOnDemand.getRandomObjects() {
        init();
        Objects obj = data.get(rnd.nextInt(data.size()));
        Integer id = obj.getIdobject();
        return Objects.findObjects(id);
    }
    
    public boolean ObjectsDataOnDemand.modifyObjects(Objects obj) {
        return false;
    }
    
    public void ObjectsDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = Objects.findObjectsEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'Objects' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<Objects>();
        for (int i = 0; i < 10; i++) {
            Objects obj = getNewTransientObjects(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
