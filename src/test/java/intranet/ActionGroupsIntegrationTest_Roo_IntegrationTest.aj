// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package intranet;

import intranet.ActionGroups;
import intranet.ActionGroupsDataOnDemand;
import intranet.ActionGroupsIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect ActionGroupsIntegrationTest_Roo_IntegrationTest {
    
    declare @type: ActionGroupsIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: ActionGroupsIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: ActionGroupsIntegrationTest: @Transactional;
    
    @Autowired
    private ActionGroupsDataOnDemand ActionGroupsIntegrationTest.dod;
    
    @Test
    public void ActionGroupsIntegrationTest.testCountActionGroupses() {
        Assert.assertNotNull("Data on demand for 'ActionGroups' failed to initialize correctly", dod.getRandomActionGroups());
        long count = ActionGroups.countActionGroupses();
        Assert.assertTrue("Counter for 'ActionGroups' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void ActionGroupsIntegrationTest.testFindActionGroups() {
        ActionGroups obj = dod.getRandomActionGroups();
        Assert.assertNotNull("Data on demand for 'ActionGroups' failed to initialize correctly", obj);
        Integer id = obj.getIdactionGroup();
        Assert.assertNotNull("Data on demand for 'ActionGroups' failed to provide an identifier", id);
        obj = ActionGroups.findActionGroups(id);
        Assert.assertNotNull("Find method for 'ActionGroups' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'ActionGroups' returned the incorrect identifier", id, obj.getIdactionGroup());
    }
    
    @Test
    public void ActionGroupsIntegrationTest.testFindAllActionGroupses() {
        Assert.assertNotNull("Data on demand for 'ActionGroups' failed to initialize correctly", dod.getRandomActionGroups());
        long count = ActionGroups.countActionGroupses();
        Assert.assertTrue("Too expensive to perform a find all test for 'ActionGroups', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<ActionGroups> result = ActionGroups.findAllActionGroupses();
        Assert.assertNotNull("Find all method for 'ActionGroups' illegally returned null", result);
        Assert.assertTrue("Find all method for 'ActionGroups' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void ActionGroupsIntegrationTest.testFindActionGroupsEntries() {
        Assert.assertNotNull("Data on demand for 'ActionGroups' failed to initialize correctly", dod.getRandomActionGroups());
        long count = ActionGroups.countActionGroupses();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<ActionGroups> result = ActionGroups.findActionGroupsEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'ActionGroups' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'ActionGroups' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void ActionGroupsIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'ActionGroups' failed to initialize correctly", dod.getRandomActionGroups());
        ActionGroups obj = dod.getNewTransientActionGroups(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'ActionGroups' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'ActionGroups' identifier to be null", obj.getIdactionGroup());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'ActionGroups' identifier to no longer be null", obj.getIdactionGroup());
    }
    
    @Test
    public void ActionGroupsIntegrationTest.testRemove() {
        ActionGroups obj = dod.getRandomActionGroups();
        Assert.assertNotNull("Data on demand for 'ActionGroups' failed to initialize correctly", obj);
        Integer id = obj.getIdactionGroup();
        Assert.assertNotNull("Data on demand for 'ActionGroups' failed to provide an identifier", id);
        obj = ActionGroups.findActionGroups(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'ActionGroups' with identifier '" + id + "'", ActionGroups.findActionGroups(id));
    }
    
}
