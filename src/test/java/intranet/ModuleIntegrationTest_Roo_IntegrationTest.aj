// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package intranet;

import intranet.Module;
import intranet.ModuleDataOnDemand;
import intranet.ModuleIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect ModuleIntegrationTest_Roo_IntegrationTest {
    
    declare @type: ModuleIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: ModuleIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: ModuleIntegrationTest: @Transactional;
    
    @Autowired
    private ModuleDataOnDemand ModuleIntegrationTest.dod;
    
    @Test
    public void ModuleIntegrationTest.testCountModules() {
        Assert.assertNotNull("Data on demand for 'Module' failed to initialize correctly", dod.getRandomModule());
        long count = Module.countModules();
        Assert.assertTrue("Counter for 'Module' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void ModuleIntegrationTest.testFindModule() {
        Module obj = dod.getRandomModule();
        Assert.assertNotNull("Data on demand for 'Module' failed to initialize correctly", obj);
        Integer id = obj.getIdmodule();
        Assert.assertNotNull("Data on demand for 'Module' failed to provide an identifier", id);
        obj = Module.findModule(id);
        Assert.assertNotNull("Find method for 'Module' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Module' returned the incorrect identifier", id, obj.getIdmodule());
    }
    
    @Test
    public void ModuleIntegrationTest.testFindAllModules() {
        Assert.assertNotNull("Data on demand for 'Module' failed to initialize correctly", dod.getRandomModule());
        long count = Module.countModules();
        Assert.assertTrue("Too expensive to perform a find all test for 'Module', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Module> result = Module.findAllModules();
        Assert.assertNotNull("Find all method for 'Module' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Module' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void ModuleIntegrationTest.testFindModuleEntries() {
        Assert.assertNotNull("Data on demand for 'Module' failed to initialize correctly", dod.getRandomModule());
        long count = Module.countModules();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Module> result = Module.findModuleEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Module' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Module' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void ModuleIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'Module' failed to initialize correctly", dod.getRandomModule());
        Module obj = dod.getNewTransientModule(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Module' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Module' identifier to be null", obj.getIdmodule());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'Module' identifier to no longer be null", obj.getIdmodule());
    }
    
    @Test
    public void ModuleIntegrationTest.testRemove() {
        Module obj = dod.getRandomModule();
        Assert.assertNotNull("Data on demand for 'Module' failed to initialize correctly", obj);
        Integer id = obj.getIdmodule();
        Assert.assertNotNull("Data on demand for 'Module' failed to provide an identifier", id);
        obj = Module.findModule(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'Module' with identifier '" + id + "'", Module.findModule(id));
    }
    
}
