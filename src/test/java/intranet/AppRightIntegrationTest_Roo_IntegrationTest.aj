// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package intranet;

import intranet.AppRight;
import intranet.AppRightDataOnDemand;
import intranet.AppRightIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect AppRightIntegrationTest_Roo_IntegrationTest {
    
    declare @type: AppRightIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: AppRightIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: AppRightIntegrationTest: @Transactional;
    
    @Autowired
    private AppRightDataOnDemand AppRightIntegrationTest.dod;
    
    @Test
    public void AppRightIntegrationTest.testCountAppRights() {
        Assert.assertNotNull("Data on demand for 'AppRight' failed to initialize correctly", dod.getRandomAppRight());
        long count = AppRight.countAppRights();
        Assert.assertTrue("Counter for 'AppRight' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void AppRightIntegrationTest.testFindAppRight() {
        AppRight obj = dod.getRandomAppRight();
        Assert.assertNotNull("Data on demand for 'AppRight' failed to initialize correctly", obj);
        Integer id = obj.getRight();
        Assert.assertNotNull("Data on demand for 'AppRight' failed to provide an identifier", id);
        obj = AppRight.findAppRight(id);
        Assert.assertNotNull("Find method for 'AppRight' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'AppRight' returned the incorrect identifier", id, obj.getRight());
    }
    
    @Test
    public void AppRightIntegrationTest.testFindAllAppRights() {
        Assert.assertNotNull("Data on demand for 'AppRight' failed to initialize correctly", dod.getRandomAppRight());
        long count = AppRight.countAppRights();
        Assert.assertTrue("Too expensive to perform a find all test for 'AppRight', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<AppRight> result = AppRight.findAllAppRights();
        Assert.assertNotNull("Find all method for 'AppRight' illegally returned null", result);
        Assert.assertTrue("Find all method for 'AppRight' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void AppRightIntegrationTest.testFindAppRightEntries() {
        Assert.assertNotNull("Data on demand for 'AppRight' failed to initialize correctly", dod.getRandomAppRight());
        long count = AppRight.countAppRights();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<AppRight> result = AppRight.findAppRightEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'AppRight' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'AppRight' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void AppRightIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'AppRight' failed to initialize correctly", dod.getRandomAppRight());
        AppRight obj = dod.getNewTransientAppRight(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'AppRight' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'AppRight' identifier to be null", obj.getRight());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'AppRight' identifier to no longer be null", obj.getRight());
    }
    
    @Test
    public void AppRightIntegrationTest.testRemove() {
        AppRight obj = dod.getRandomAppRight();
        Assert.assertNotNull("Data on demand for 'AppRight' failed to initialize correctly", obj);
        Integer id = obj.getRight();
        Assert.assertNotNull("Data on demand for 'AppRight' failed to provide an identifier", id);
        obj = AppRight.findAppRight(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'AppRight' with identifier '" + id + "'", AppRight.findAppRight(id));
    }
    
}
