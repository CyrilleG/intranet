// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package intranet;

import intranet.GroupFilter;
import intranet.GroupFilterDataOnDemand;
import intranet.GroupFilterIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect GroupFilterIntegrationTest_Roo_IntegrationTest {
    
    declare @type: GroupFilterIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: GroupFilterIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: GroupFilterIntegrationTest: @Transactional;
    
    @Autowired
    private GroupFilterDataOnDemand GroupFilterIntegrationTest.dod;
    
    @Test
    public void GroupFilterIntegrationTest.testCountGroupFilters() {
        Assert.assertNotNull("Data on demand for 'GroupFilter' failed to initialize correctly", dod.getRandomGroupFilter());
        long count = GroupFilter.countGroupFilters();
        Assert.assertTrue("Counter for 'GroupFilter' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void GroupFilterIntegrationTest.testFindGroupFilter() {
        GroupFilter obj = dod.getRandomGroupFilter();
        Assert.assertNotNull("Data on demand for 'GroupFilter' failed to initialize correctly", obj);
        Integer id = obj.getIdgroupFilter();
        Assert.assertNotNull("Data on demand for 'GroupFilter' failed to provide an identifier", id);
        obj = GroupFilter.findGroupFilter(id);
        Assert.assertNotNull("Find method for 'GroupFilter' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'GroupFilter' returned the incorrect identifier", id, obj.getIdgroupFilter());
    }
    
    @Test
    public void GroupFilterIntegrationTest.testFindAllGroupFilters() {
        Assert.assertNotNull("Data on demand for 'GroupFilter' failed to initialize correctly", dod.getRandomGroupFilter());
        long count = GroupFilter.countGroupFilters();
        Assert.assertTrue("Too expensive to perform a find all test for 'GroupFilter', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<GroupFilter> result = GroupFilter.findAllGroupFilters();
        Assert.assertNotNull("Find all method for 'GroupFilter' illegally returned null", result);
        Assert.assertTrue("Find all method for 'GroupFilter' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void GroupFilterIntegrationTest.testFindGroupFilterEntries() {
        Assert.assertNotNull("Data on demand for 'GroupFilter' failed to initialize correctly", dod.getRandomGroupFilter());
        long count = GroupFilter.countGroupFilters();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<GroupFilter> result = GroupFilter.findGroupFilterEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'GroupFilter' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'GroupFilter' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void GroupFilterIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'GroupFilter' failed to initialize correctly", dod.getRandomGroupFilter());
        GroupFilter obj = dod.getNewTransientGroupFilter(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'GroupFilter' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'GroupFilter' identifier to be null", obj.getIdgroupFilter());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'GroupFilter' identifier to no longer be null", obj.getIdgroupFilter());
    }
    
    @Test
    public void GroupFilterIntegrationTest.testRemove() {
        GroupFilter obj = dod.getRandomGroupFilter();
        Assert.assertNotNull("Data on demand for 'GroupFilter' failed to initialize correctly", obj);
        Integer id = obj.getIdgroupFilter();
        Assert.assertNotNull("Data on demand for 'GroupFilter' failed to provide an identifier", id);
        obj = GroupFilter.findGroupFilter(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'GroupFilter' with identifier '" + id + "'", GroupFilter.findGroupFilter(id));
    }
    
}
