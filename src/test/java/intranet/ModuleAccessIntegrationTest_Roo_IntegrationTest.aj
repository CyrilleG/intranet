// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package intranet;

import intranet.ModuleAccess;
import intranet.ModuleAccessDataOnDemand;
import intranet.ModuleAccessIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect ModuleAccessIntegrationTest_Roo_IntegrationTest {
    
    declare @type: ModuleAccessIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: ModuleAccessIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: ModuleAccessIntegrationTest: @Transactional;
    
    @Autowired
    private ModuleAccessDataOnDemand ModuleAccessIntegrationTest.dod;
    
    @Test
    public void ModuleAccessIntegrationTest.testCountModuleAccesses() {
        Assert.assertNotNull("Data on demand for 'ModuleAccess' failed to initialize correctly", dod.getRandomModuleAccess());
        long count = ModuleAccess.countModuleAccesses();
        Assert.assertTrue("Counter for 'ModuleAccess' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void ModuleAccessIntegrationTest.testFindModuleAccess() {
        ModuleAccess obj = dod.getRandomModuleAccess();
        Assert.assertNotNull("Data on demand for 'ModuleAccess' failed to initialize correctly", obj);
        Integer id = obj.getIdmoduleAccess();
        Assert.assertNotNull("Data on demand for 'ModuleAccess' failed to provide an identifier", id);
        obj = ModuleAccess.findModuleAccess(id);
        Assert.assertNotNull("Find method for 'ModuleAccess' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'ModuleAccess' returned the incorrect identifier", id, obj.getIdmoduleAccess());
    }
    
    @Test
    public void ModuleAccessIntegrationTest.testFindAllModuleAccesses() {
        Assert.assertNotNull("Data on demand for 'ModuleAccess' failed to initialize correctly", dod.getRandomModuleAccess());
        long count = ModuleAccess.countModuleAccesses();
        Assert.assertTrue("Too expensive to perform a find all test for 'ModuleAccess', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<ModuleAccess> result = ModuleAccess.findAllModuleAccesses();
        Assert.assertNotNull("Find all method for 'ModuleAccess' illegally returned null", result);
        Assert.assertTrue("Find all method for 'ModuleAccess' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void ModuleAccessIntegrationTest.testFindModuleAccessEntries() {
        Assert.assertNotNull("Data on demand for 'ModuleAccess' failed to initialize correctly", dod.getRandomModuleAccess());
        long count = ModuleAccess.countModuleAccesses();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<ModuleAccess> result = ModuleAccess.findModuleAccessEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'ModuleAccess' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'ModuleAccess' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void ModuleAccessIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'ModuleAccess' failed to initialize correctly", dod.getRandomModuleAccess());
        ModuleAccess obj = dod.getNewTransientModuleAccess(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'ModuleAccess' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'ModuleAccess' identifier to be null", obj.getIdmoduleAccess());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'ModuleAccess' identifier to no longer be null", obj.getIdmoduleAccess());
    }
    
    @Test
    public void ModuleAccessIntegrationTest.testRemove() {
        ModuleAccess obj = dod.getRandomModuleAccess();
        Assert.assertNotNull("Data on demand for 'ModuleAccess' failed to initialize correctly", obj);
        Integer id = obj.getIdmoduleAccess();
        Assert.assertNotNull("Data on demand for 'ModuleAccess' failed to provide an identifier", id);
        obj = ModuleAccess.findModuleAccess(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'ModuleAccess' with identifier '" + id + "'", ModuleAccess.findModuleAccess(id));
    }
    
}
