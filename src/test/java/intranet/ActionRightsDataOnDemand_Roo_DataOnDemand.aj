// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package intranet;

import intranet.Action;
import intranet.ActionDataOnDemand;
import intranet.ActionRights;
import intranet.ActionRightsDataOnDemand;
import intranet.Right;
import intranet.RightDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

privileged aspect ActionRightsDataOnDemand_Roo_DataOnDemand {
    
    declare @type: ActionRightsDataOnDemand: @Component;
    
    private Random ActionRightsDataOnDemand.rnd = new SecureRandom();
    
    private List<ActionRights> ActionRightsDataOnDemand.data;
    
    @Autowired
    private ActionDataOnDemand ActionRightsDataOnDemand.actionDataOnDemand;
    
    @Autowired
    private RightDataOnDemand ActionRightsDataOnDemand.rightDataOnDemand;
    
    public ActionRights ActionRightsDataOnDemand.getNewTransientActionRights(int index) {
        ActionRights obj = new ActionRights();
        setIdaction(obj, index);
        setIdright(obj, index);
        return obj;
    }
    
    public void ActionRightsDataOnDemand.setIdaction(ActionRights obj, int index) {
        Action idaction = actionDataOnDemand.getRandomAction();
        obj.setIdaction(idaction);
    }
    
    public void ActionRightsDataOnDemand.setIdright(ActionRights obj, int index) {
        Right idright = rightDataOnDemand.getRandomRight();
        obj.setIdright(idright);
    }
    
    public ActionRights ActionRightsDataOnDemand.getSpecificActionRights(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        ActionRights obj = data.get(index);
        Integer id = obj.getIdactionRights();
        return ActionRights.findActionRights(id);
    }
    
    public ActionRights ActionRightsDataOnDemand.getRandomActionRights() {
        init();
        ActionRights obj = data.get(rnd.nextInt(data.size()));
        Integer id = obj.getIdactionRights();
        return ActionRights.findActionRights(id);
    }
    
    public boolean ActionRightsDataOnDemand.modifyActionRights(ActionRights obj) {
        return false;
    }
    
    public void ActionRightsDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = ActionRights.findActionRightsEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'ActionRights' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<ActionRights>();
        for (int i = 0; i < 10; i++) {
            ActionRights obj = getNewTransientActionRights(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
