// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package intranet;

import intranet.AppConfig;
import intranet.AppConfigDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.stereotype.Component;

privileged aspect AppConfigDataOnDemand_Roo_DataOnDemand {
    
    declare @type: AppConfigDataOnDemand: @Component;
    
    private Random AppConfigDataOnDemand.rnd = new SecureRandom();
    
    private List<AppConfig> AppConfigDataOnDemand.data;
    
    public AppConfig AppConfigDataOnDemand.getNewTransientAppConfig(int index) {
        AppConfig obj = new AppConfig();
        setKey(obj, index);
        setValue(obj, index);
        return obj;
    }
    
    public void AppConfigDataOnDemand.setKey(AppConfig obj, int index) {
        String key = "key_" + index;
        if (key.length() > 100) {
            key = key.substring(0, 100);
        }
        obj.setKey(key);
    }
    
    public void AppConfigDataOnDemand.setValue(AppConfig obj, int index) {
        String value = "value_" + index;
        if (value.length() > 255) {
            value = value.substring(0, 255);
        }
        obj.setValue(value);
    }
    
    public AppConfig AppConfigDataOnDemand.getSpecificAppConfig(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        AppConfig obj = data.get(index);
        Integer id = obj.getAppConfig();
        return AppConfig.findAppConfig(id);
    }
    
    public AppConfig AppConfigDataOnDemand.getRandomAppConfig() {
        init();
        AppConfig obj = data.get(rnd.nextInt(data.size()));
        Integer id = obj.getAppConfig();
        return AppConfig.findAppConfig(id);
    }
    
    public boolean AppConfigDataOnDemand.modifyAppConfig(AppConfig obj) {
        return false;
    }
    
    public void AppConfigDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = AppConfig.findAppConfigEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'AppConfig' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<AppConfig>();
        for (int i = 0; i < 10; i++) {
            AppConfig obj = getNewTransientAppConfig(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
