// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package intranet;

import intranet.Right;
import intranet.RightDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.stereotype.Component;

privileged aspect RightDataOnDemand_Roo_DataOnDemand {
    
    declare @type: RightDataOnDemand: @Component;
    
    private Random RightDataOnDemand.rnd = new SecureRandom();
    
    private List<Right> RightDataOnDemand.data;
    
    public Right RightDataOnDemand.getNewTransientRight(int index) {
        Right obj = new Right();
        setDescription(obj, index);
        setIdent(obj, index);
        setName(obj, index);
        return obj;
    }
    
    public void RightDataOnDemand.setDescription(Right obj, int index) {
        String description = "description_" + index;
        obj.setDescription(description);
    }
    
    public void RightDataOnDemand.setIdent(Right obj, int index) {
        String ident = "ident_" + index;
        if (ident.length() > 70) {
            ident = new Random().nextInt(10) + ident.substring(1, 70);
        }
        obj.setIdent(ident);
    }
    
    public void RightDataOnDemand.setName(Right obj, int index) {
        String name = "name_" + index;
        if (name.length() > 100) {
            name = name.substring(0, 100);
        }
        obj.setName(name);
    }
    
    public Right RightDataOnDemand.getSpecificRight(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        Right obj = data.get(index);
        Integer id = obj.getIdright();
        return Right.findRight(id);
    }
    
    public Right RightDataOnDemand.getRandomRight() {
        init();
        Right obj = data.get(rnd.nextInt(data.size()));
        Integer id = obj.getIdright();
        return Right.findRight(id);
    }
    
    public boolean RightDataOnDemand.modifyRight(Right obj) {
        return false;
    }
    
    public void RightDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = Right.findRightEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'Right' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<Right>();
        for (int i = 0; i < 10; i++) {
            Right obj = getNewTransientRight(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
