// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package intranet;

import intranet.GroupFilters;
import intranet.GroupFiltersDataOnDemand;
import intranet.GroupFiltersIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect GroupFiltersIntegrationTest_Roo_IntegrationTest {
    
    declare @type: GroupFiltersIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: GroupFiltersIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: GroupFiltersIntegrationTest: @Transactional;
    
    @Autowired
    private GroupFiltersDataOnDemand GroupFiltersIntegrationTest.dod;
    
    @Test
    public void GroupFiltersIntegrationTest.testCountGroupFilterses() {
        Assert.assertNotNull("Data on demand for 'GroupFilters' failed to initialize correctly", dod.getRandomGroupFilters());
        long count = GroupFilters.countGroupFilterses();
        Assert.assertTrue("Counter for 'GroupFilters' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void GroupFiltersIntegrationTest.testFindGroupFilters() {
        GroupFilters obj = dod.getRandomGroupFilters();
        Assert.assertNotNull("Data on demand for 'GroupFilters' failed to initialize correctly", obj);
        Integer id = obj.getIdgroupFilter();
        Assert.assertNotNull("Data on demand for 'GroupFilters' failed to provide an identifier", id);
        obj = GroupFilters.findGroupFilters(id);
        Assert.assertNotNull("Find method for 'GroupFilters' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'GroupFilters' returned the incorrect identifier", id, obj.getIdgroupFilter());
    }
    
    @Test
    public void GroupFiltersIntegrationTest.testFindAllGroupFilterses() {
        Assert.assertNotNull("Data on demand for 'GroupFilters' failed to initialize correctly", dod.getRandomGroupFilters());
        long count = GroupFilters.countGroupFilterses();
        Assert.assertTrue("Too expensive to perform a find all test for 'GroupFilters', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<GroupFilters> result = GroupFilters.findAllGroupFilterses();
        Assert.assertNotNull("Find all method for 'GroupFilters' illegally returned null", result);
        Assert.assertTrue("Find all method for 'GroupFilters' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void GroupFiltersIntegrationTest.testFindGroupFiltersEntries() {
        Assert.assertNotNull("Data on demand for 'GroupFilters' failed to initialize correctly", dod.getRandomGroupFilters());
        long count = GroupFilters.countGroupFilterses();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<GroupFilters> result = GroupFilters.findGroupFiltersEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'GroupFilters' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'GroupFilters' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void GroupFiltersIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'GroupFilters' failed to initialize correctly", dod.getRandomGroupFilters());
        GroupFilters obj = dod.getNewTransientGroupFilters(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'GroupFilters' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'GroupFilters' identifier to be null", obj.getIdgroupFilter());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'GroupFilters' identifier to no longer be null", obj.getIdgroupFilter());
    }
    
    @Test
    public void GroupFiltersIntegrationTest.testRemove() {
        GroupFilters obj = dod.getRandomGroupFilters();
        Assert.assertNotNull("Data on demand for 'GroupFilters' failed to initialize correctly", obj);
        Integer id = obj.getIdgroupFilter();
        Assert.assertNotNull("Data on demand for 'GroupFilters' failed to provide an identifier", id);
        obj = GroupFilters.findGroupFilters(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'GroupFilters' with identifier '" + id + "'", GroupFilters.findGroupFilters(id));
    }
    
}
