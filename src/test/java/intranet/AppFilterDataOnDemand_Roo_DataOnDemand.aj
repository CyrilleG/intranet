// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package intranet;

import intranet.AppFilter;
import intranet.AppFilterDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.stereotype.Component;

privileged aspect AppFilterDataOnDemand_Roo_DataOnDemand {
    
    declare @type: AppFilterDataOnDemand: @Component;
    
    private Random AppFilterDataOnDemand.rnd = new SecureRandom();
    
    private List<AppFilter> AppFilterDataOnDemand.data;
    
    public AppFilter AppFilterDataOnDemand.getNewTransientAppFilter(int index) {
        AppFilter obj = new AppFilter();
        setClass1(obj, index);
        setDescription(obj, index);
        setName(obj, index);
        return obj;
    }
    
    public void AppFilterDataOnDemand.setClass1(AppFilter obj, int index) {
        String class1 = "class1_" + index;
        if (class1.length() > 75) {
            class1 = class1.substring(0, 75);
        }
        obj.setClass1(class1);
    }
    
    public void AppFilterDataOnDemand.setDescription(AppFilter obj, int index) {
        String description = "description_" + index;
        if (description.length() > 255) {
            description = description.substring(0, 255);
        }
        obj.setDescription(description);
    }
    
    public void AppFilterDataOnDemand.setName(AppFilter obj, int index) {
        String name = "name_" + index;
        if (name.length() > 100) {
            name = name.substring(0, 100);
        }
        obj.setName(name);
    }
    
    public AppFilter AppFilterDataOnDemand.getSpecificAppFilter(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        AppFilter obj = data.get(index);
        Integer id = obj.getFilter();
        return AppFilter.findAppFilter(id);
    }
    
    public AppFilter AppFilterDataOnDemand.getRandomAppFilter() {
        init();
        AppFilter obj = data.get(rnd.nextInt(data.size()));
        Integer id = obj.getFilter();
        return AppFilter.findAppFilter(id);
    }
    
    public boolean AppFilterDataOnDemand.modifyAppFilter(AppFilter obj) {
        return false;
    }
    
    public void AppFilterDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = AppFilter.findAppFilterEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'AppFilter' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<AppFilter>();
        for (int i = 0; i < 10; i++) {
            AppFilter obj = getNewTransientAppFilter(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
