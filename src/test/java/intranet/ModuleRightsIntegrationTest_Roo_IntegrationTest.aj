// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package intranet;

import intranet.ModuleRights;
import intranet.ModuleRightsDataOnDemand;
import intranet.ModuleRightsIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect ModuleRightsIntegrationTest_Roo_IntegrationTest {
    
    declare @type: ModuleRightsIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: ModuleRightsIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: ModuleRightsIntegrationTest: @Transactional;
    
    @Autowired
    private ModuleRightsDataOnDemand ModuleRightsIntegrationTest.dod;
    
    @Test
    public void ModuleRightsIntegrationTest.testCountModuleRightses() {
        Assert.assertNotNull("Data on demand for 'ModuleRights' failed to initialize correctly", dod.getRandomModuleRights());
        long count = ModuleRights.countModuleRightses();
        Assert.assertTrue("Counter for 'ModuleRights' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void ModuleRightsIntegrationTest.testFindModuleRights() {
        ModuleRights obj = dod.getRandomModuleRights();
        Assert.assertNotNull("Data on demand for 'ModuleRights' failed to initialize correctly", obj);
        Integer id = obj.getIdmoduleRight();
        Assert.assertNotNull("Data on demand for 'ModuleRights' failed to provide an identifier", id);
        obj = ModuleRights.findModuleRights(id);
        Assert.assertNotNull("Find method for 'ModuleRights' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'ModuleRights' returned the incorrect identifier", id, obj.getIdmoduleRight());
    }
    
    @Test
    public void ModuleRightsIntegrationTest.testFindAllModuleRightses() {
        Assert.assertNotNull("Data on demand for 'ModuleRights' failed to initialize correctly", dod.getRandomModuleRights());
        long count = ModuleRights.countModuleRightses();
        Assert.assertTrue("Too expensive to perform a find all test for 'ModuleRights', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<ModuleRights> result = ModuleRights.findAllModuleRightses();
        Assert.assertNotNull("Find all method for 'ModuleRights' illegally returned null", result);
        Assert.assertTrue("Find all method for 'ModuleRights' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void ModuleRightsIntegrationTest.testFindModuleRightsEntries() {
        Assert.assertNotNull("Data on demand for 'ModuleRights' failed to initialize correctly", dod.getRandomModuleRights());
        long count = ModuleRights.countModuleRightses();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<ModuleRights> result = ModuleRights.findModuleRightsEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'ModuleRights' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'ModuleRights' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void ModuleRightsIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'ModuleRights' failed to initialize correctly", dod.getRandomModuleRights());
        ModuleRights obj = dod.getNewTransientModuleRights(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'ModuleRights' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'ModuleRights' identifier to be null", obj.getIdmoduleRight());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'ModuleRights' identifier to no longer be null", obj.getIdmoduleRight());
    }
    
    @Test
    public void ModuleRightsIntegrationTest.testRemove() {
        ModuleRights obj = dod.getRandomModuleRights();
        Assert.assertNotNull("Data on demand for 'ModuleRights' failed to initialize correctly", obj);
        Integer id = obj.getIdmoduleRight();
        Assert.assertNotNull("Data on demand for 'ModuleRights' failed to provide an identifier", id);
        obj = ModuleRights.findModuleRights(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'ModuleRights' with identifier '" + id + "'", ModuleRights.findModuleRights(id));
    }
    
}
