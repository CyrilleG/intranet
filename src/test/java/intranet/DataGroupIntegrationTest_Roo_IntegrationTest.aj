// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package intranet;

import intranet.DataGroup;
import intranet.DataGroupDataOnDemand;
import intranet.DataGroupIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect DataGroupIntegrationTest_Roo_IntegrationTest {
    
    declare @type: DataGroupIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: DataGroupIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: DataGroupIntegrationTest: @Transactional;
    
    @Autowired
    private DataGroupDataOnDemand DataGroupIntegrationTest.dod;
    
    @Test
    public void DataGroupIntegrationTest.testCountDataGroups() {
        Assert.assertNotNull("Data on demand for 'DataGroup' failed to initialize correctly", dod.getRandomDataGroup());
        long count = DataGroup.countDataGroups();
        Assert.assertTrue("Counter for 'DataGroup' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void DataGroupIntegrationTest.testFindDataGroup() {
        DataGroup obj = dod.getRandomDataGroup();
        Assert.assertNotNull("Data on demand for 'DataGroup' failed to initialize correctly", obj);
        Integer id = obj.getDataGroup();
        Assert.assertNotNull("Data on demand for 'DataGroup' failed to provide an identifier", id);
        obj = DataGroup.findDataGroup(id);
        Assert.assertNotNull("Find method for 'DataGroup' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'DataGroup' returned the incorrect identifier", id, obj.getDataGroup());
    }
    
    @Test
    public void DataGroupIntegrationTest.testFindAllDataGroups() {
        Assert.assertNotNull("Data on demand for 'DataGroup' failed to initialize correctly", dod.getRandomDataGroup());
        long count = DataGroup.countDataGroups();
        Assert.assertTrue("Too expensive to perform a find all test for 'DataGroup', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<DataGroup> result = DataGroup.findAllDataGroups();
        Assert.assertNotNull("Find all method for 'DataGroup' illegally returned null", result);
        Assert.assertTrue("Find all method for 'DataGroup' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void DataGroupIntegrationTest.testFindDataGroupEntries() {
        Assert.assertNotNull("Data on demand for 'DataGroup' failed to initialize correctly", dod.getRandomDataGroup());
        long count = DataGroup.countDataGroups();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<DataGroup> result = DataGroup.findDataGroupEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'DataGroup' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'DataGroup' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void DataGroupIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'DataGroup' failed to initialize correctly", dod.getRandomDataGroup());
        DataGroup obj = dod.getNewTransientDataGroup(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'DataGroup' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'DataGroup' identifier to be null", obj.getDataGroup());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'DataGroup' identifier to no longer be null", obj.getDataGroup());
    }
    
    @Test
    public void DataGroupIntegrationTest.testRemove() {
        DataGroup obj = dod.getRandomDataGroup();
        Assert.assertNotNull("Data on demand for 'DataGroup' failed to initialize correctly", obj);
        Integer id = obj.getDataGroup();
        Assert.assertNotNull("Data on demand for 'DataGroup' failed to provide an identifier", id);
        obj = DataGroup.findDataGroup(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'DataGroup' with identifier '" + id + "'", DataGroup.findDataGroup(id));
    }
    
}
