// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package intranet;

import intranet.Filter;
import intranet.FilterDataOnDemand;
import intranet.FilterIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect FilterIntegrationTest_Roo_IntegrationTest {
    
    declare @type: FilterIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: FilterIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: FilterIntegrationTest: @Transactional;
    
    @Autowired
    private FilterDataOnDemand FilterIntegrationTest.dod;
    
    @Test
    public void FilterIntegrationTest.testCountFilters() {
        Assert.assertNotNull("Data on demand for 'Filter' failed to initialize correctly", dod.getRandomFilter());
        long count = Filter.countFilters();
        Assert.assertTrue("Counter for 'Filter' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void FilterIntegrationTest.testFindFilter() {
        Filter obj = dod.getRandomFilter();
        Assert.assertNotNull("Data on demand for 'Filter' failed to initialize correctly", obj);
        Integer id = obj.getIdfilter();
        Assert.assertNotNull("Data on demand for 'Filter' failed to provide an identifier", id);
        obj = Filter.findFilter(id);
        Assert.assertNotNull("Find method for 'Filter' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Filter' returned the incorrect identifier", id, obj.getIdfilter());
    }
    
    @Test
    public void FilterIntegrationTest.testFindAllFilters() {
        Assert.assertNotNull("Data on demand for 'Filter' failed to initialize correctly", dod.getRandomFilter());
        long count = Filter.countFilters();
        Assert.assertTrue("Too expensive to perform a find all test for 'Filter', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Filter> result = Filter.findAllFilters();
        Assert.assertNotNull("Find all method for 'Filter' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Filter' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void FilterIntegrationTest.testFindFilterEntries() {
        Assert.assertNotNull("Data on demand for 'Filter' failed to initialize correctly", dod.getRandomFilter());
        long count = Filter.countFilters();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Filter> result = Filter.findFilterEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Filter' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Filter' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void FilterIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'Filter' failed to initialize correctly", dod.getRandomFilter());
        Filter obj = dod.getNewTransientFilter(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Filter' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Filter' identifier to be null", obj.getIdfilter());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'Filter' identifier to no longer be null", obj.getIdfilter());
    }
    
    @Test
    public void FilterIntegrationTest.testRemove() {
        Filter obj = dod.getRandomFilter();
        Assert.assertNotNull("Data on demand for 'Filter' failed to initialize correctly", obj);
        Integer id = obj.getIdfilter();
        Assert.assertNotNull("Data on demand for 'Filter' failed to provide an identifier", id);
        obj = Filter.findFilter(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'Filter' with identifier '" + id + "'", Filter.findFilter(id));
    }
    
}
