// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package intranet;

import intranet.Group;
import intranet.GroupDataOnDemand;
import intranet.Module;
import intranet.ModuleDataOnDemand;
import intranet.ModuleGroups;
import intranet.ModuleGroupsDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

privileged aspect ModuleGroupsDataOnDemand_Roo_DataOnDemand {
    
    declare @type: ModuleGroupsDataOnDemand: @Component;
    
    private Random ModuleGroupsDataOnDemand.rnd = new SecureRandom();
    
    private List<ModuleGroups> ModuleGroupsDataOnDemand.data;
    
    @Autowired
    private GroupDataOnDemand ModuleGroupsDataOnDemand.groupDataOnDemand;
    
    @Autowired
    private ModuleDataOnDemand ModuleGroupsDataOnDemand.moduleDataOnDemand;
    
    public ModuleGroups ModuleGroupsDataOnDemand.getNewTransientModuleGroups(int index) {
        ModuleGroups obj = new ModuleGroups();
        setIdgroup(obj, index);
        setIdmodule(obj, index);
        return obj;
    }
    
    public void ModuleGroupsDataOnDemand.setIdgroup(ModuleGroups obj, int index) {
        Group idgroup = groupDataOnDemand.getRandomGroup();
        obj.setIdgroup(idgroup);
    }
    
    public void ModuleGroupsDataOnDemand.setIdmodule(ModuleGroups obj, int index) {
        Module idmodule = moduleDataOnDemand.getRandomModule();
        obj.setIdmodule(idmodule);
    }
    
    public ModuleGroups ModuleGroupsDataOnDemand.getSpecificModuleGroups(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        ModuleGroups obj = data.get(index);
        Integer id = obj.getIdmoduleAccess();
        return ModuleGroups.findModuleGroups(id);
    }
    
    public ModuleGroups ModuleGroupsDataOnDemand.getRandomModuleGroups() {
        init();
        ModuleGroups obj = data.get(rnd.nextInt(data.size()));
        Integer id = obj.getIdmoduleAccess();
        return ModuleGroups.findModuleGroups(id);
    }
    
    public boolean ModuleGroupsDataOnDemand.modifyModuleGroups(ModuleGroups obj) {
        return false;
    }
    
    public void ModuleGroupsDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = ModuleGroups.findModuleGroupsEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'ModuleGroups' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<ModuleGroups>();
        for (int i = 0; i < 10; i++) {
            ModuleGroups obj = getNewTransientModuleGroups(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
