// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package intranet;

import intranet.AppModule;
import intranet.AppModuleDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.stereotype.Component;

privileged aspect AppModuleDataOnDemand_Roo_DataOnDemand {
    
    declare @type: AppModuleDataOnDemand: @Component;
    
    private Random AppModuleDataOnDemand.rnd = new SecureRandom();
    
    private List<AppModule> AppModuleDataOnDemand.data;
    
    public AppModule AppModuleDataOnDemand.getNewTransientAppModule(int index) {
        AppModule obj = new AppModule();
        setClass1(obj, index);
        setDescription(obj, index);
        setEnabled(obj, index);
        setName(obj, index);
        return obj;
    }
    
    public void AppModuleDataOnDemand.setClass1(AppModule obj, int index) {
        String class1 = "class1_" + index;
        if (class1.length() > 100) {
            class1 = class1.substring(0, 100);
        }
        obj.setClass1(class1);
    }
    
    public void AppModuleDataOnDemand.setDescription(AppModule obj, int index) {
        String description = "description_" + index;
        if (description.length() > 255) {
            description = description.substring(0, 255);
        }
        obj.setDescription(description);
    }
    
    public void AppModuleDataOnDemand.setEnabled(AppModule obj, int index) {
        Boolean enabled = true;
        obj.setEnabled(enabled);
    }
    
    public void AppModuleDataOnDemand.setName(AppModule obj, int index) {
        String name = "name_" + index;
        if (name.length() > 100) {
            name = new Random().nextInt(10) + name.substring(1, 100);
        }
        obj.setName(name);
    }
    
    public AppModule AppModuleDataOnDemand.getSpecificAppModule(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        AppModule obj = data.get(index);
        Integer id = obj.getModule();
        return AppModule.findAppModule(id);
    }
    
    public AppModule AppModuleDataOnDemand.getRandomAppModule() {
        init();
        AppModule obj = data.get(rnd.nextInt(data.size()));
        Integer id = obj.getModule();
        return AppModule.findAppModule(id);
    }
    
    public boolean AppModuleDataOnDemand.modifyAppModule(AppModule obj) {
        return false;
    }
    
    public void AppModuleDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = AppModule.findAppModuleEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'AppModule' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<AppModule>();
        for (int i = 0; i < 10; i++) {
            AppModule obj = getNewTransientAppModule(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
