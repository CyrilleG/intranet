// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package intranet;

import intranet.Group;
import intranet.GroupDataOnDemand;
import intranet.GroupRights;
import intranet.GroupRightsDataOnDemand;
import intranet.Right;
import intranet.RightDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

privileged aspect GroupRightsDataOnDemand_Roo_DataOnDemand {
    
    declare @type: GroupRightsDataOnDemand: @Component;
    
    private Random GroupRightsDataOnDemand.rnd = new SecureRandom();
    
    private List<GroupRights> GroupRightsDataOnDemand.data;
    
    @Autowired
    private GroupDataOnDemand GroupRightsDataOnDemand.groupDataOnDemand;
    
    @Autowired
    private RightDataOnDemand GroupRightsDataOnDemand.rightDataOnDemand;
    
    public GroupRights GroupRightsDataOnDemand.getNewTransientGroupRights(int index) {
        GroupRights obj = new GroupRights();
        setIdgroup(obj, index);
        setIdright(obj, index);
        return obj;
    }
    
    public void GroupRightsDataOnDemand.setIdgroup(GroupRights obj, int index) {
        Group idgroup = groupDataOnDemand.getRandomGroup();
        obj.setIdgroup(idgroup);
    }
    
    public void GroupRightsDataOnDemand.setIdright(GroupRights obj, int index) {
        Right idright = rightDataOnDemand.getRandomRight();
        obj.setIdright(idright);
    }
    
    public GroupRights GroupRightsDataOnDemand.getSpecificGroupRights(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        GroupRights obj = data.get(index);
        Integer id = obj.getIdgroupRights();
        return GroupRights.findGroupRights(id);
    }
    
    public GroupRights GroupRightsDataOnDemand.getRandomGroupRights() {
        init();
        GroupRights obj = data.get(rnd.nextInt(data.size()));
        Integer id = obj.getIdgroupRights();
        return GroupRights.findGroupRights(id);
    }
    
    public boolean GroupRightsDataOnDemand.modifyGroupRights(GroupRights obj) {
        return false;
    }
    
    public void GroupRightsDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = GroupRights.findGroupRightsEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'GroupRights' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<GroupRights>();
        for (int i = 0; i < 10; i++) {
            GroupRights obj = getNewTransientGroupRights(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
